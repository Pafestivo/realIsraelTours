import React from "react";
import "@/styles/mobileStickyFooter.css";

const MobileStickyFooter = () => {
  return (
    <div className="sticky-footer mobile-only">
      <button className="btn1">Request Callback</button>
      <button>Inquire Now</button>
    </div>
  );
};

export default MobileStickyFooter;
